<UserControl x:Class="ViceCodeTestTask.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViceCodeTestTask.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="419*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                    Grid.Row="0">
            <Button Content="Settings" Width="80" Margin="2" Command="{Binding ToUserSettingsCommand}"/>
            <Button Content="Log out" Width="80" Margin="2" Command="{Binding ToLogInCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <TextBlock Text="Account info: " />
            <TextBlock Text="{Binding Username, FallbackValue=USERNAME}" />
            <TextBlock Text="{Binding Email, FallbackValue=EMAIL}" />
            <TextBlock Text="{Binding SingedUpSince, FallbackValue=SINCE}" />
        </StackPanel>

    </Grid>
</UserControl>
